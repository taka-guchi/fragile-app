= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
            url: user_path, local: true, method: :delete) do | f |
  = f.submit 'Unfollow', class: 'btn btn-danger'
